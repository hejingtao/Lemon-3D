/**
 * ------------------------------------------------------------------
 * Community模块 控制器
 * ------------------------------------------------------------------
 */
app.controller('community', function($scope, $rootScope, $timeout, $state, $http, $rootScope, $ocLazyLoad,  authService, tools, ENV) {
  
  $rootScope.bodyState = 'index';  

})